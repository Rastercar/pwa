mutation registerUserMutation($user: RegisterUserDTO!) {
  register(user: $user) {
    token {
      value
      type
    }
    user {
      ... on UserModel {
        ...FullUser
      }
      ... on MasterUserModel {
        ...FullMasterUser
      }
    }
  }
}
