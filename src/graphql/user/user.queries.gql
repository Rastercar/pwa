query currentUser {
  me {
    ... on UserModel {
      ...FullUser
    }
    ... on MasterUserModel {
      ...FullMasterUser
    }
  }
}

query userById($id: Int!) {
  user(id: $id) {
    ...FullUser
  }
}
